# Do not use Needle's default Hibernate configuration (needle-hsql-hibernate.cfg.xml).
# Use the one provided by our persistence.xml instead.
hibernate.cfg.filename=

# R.I.P. EasyMock
mock.provider=de.akquinet.jbosscc.needle.mock.MockitoProvider